export const DEFAULT_SELECTED_METRIC_KEYS = [
  "ballSpeed",
  "spinRate",
  "shoulderRotation",
  "hipRotationSpeed",
] as const;

export const DEFAULT_SELECTED_SCORE_SECTIONS = [
  "Technical (Biomechanics)",
  "Tactical",
  "Movement",
] as const;